name: CI/CD
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  release:
    name: Release
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
    - name: Checkout
      uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: 1.26

    - name: Run semantic release
      uses: cycjimmy/semantic-release-action@v6
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: 1.26
        cache: true

    - name: Checkout code
      uses: actions/checkout@v7

    - name: Install Chrome
      id: setup-chrome
      uses: browser-actions/setup-chrome@v2
      with:
        chrome-version: stable

    - name: Get dependencies
      run: go mod download

    - name: Run Tests
      env:
        CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
      run: go test -v ./...

  build:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    needs: release
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        goos: [linux, darwin]
        goarch: [amd64, arm64]

    steps:
    - name: Set up Go 1.26
      uses: actions/setup-go@v7
      with:
        go-version: 1.26
        cache: true
      id: go

    - name: Check out code
      uses: actions/checkout@v7

    - name: Get dependencies
      run: go mod download

    - name: Building
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
        CGO_ENABLED: 0
      run: |
        OUTPUT_NAME=bin/comicbookcollector-${{ matrix.goos }}-${{ matrix.goarch }}

        go build -v -o $OUTPUT_NAME ./cmd/scraper

    - name: Upload Artifact
      uses: actions/upload-artifact@v7
      with:
        name: comicbookcollector-${{ matrix.goos }}-${{ matrix.goarch }}
        path: bin/comicbookcollector-*
        # Set retention to keep artifacts clean
        retention-days: 5
